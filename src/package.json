{
  "name": "nava",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx eleventy --config=eleventy.config.js",
    "serve": "npx eleventy --config=eleventy.config.js --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.8.2"
  },
  "dependencies": {
    "clean-css": "^4.2.1",
    "html-minifier": "^4.0.0",
    "uglify-es": "^3.3.9"
  }
}
